treatment()
  box-shadow: inset 0 0 .5rem rgba($colors.blue, .3)
  border-radius: 1rem
  background-color: rgba(black, .5)
  border-right: 2px solid rgba($colors.blue, .3)
  border-left: 2px solid rgba($colors.blue, .3)
